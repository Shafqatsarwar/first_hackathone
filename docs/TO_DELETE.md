# Candidate files for archival or deletion

The following `docs/` files are used for testing/demos. Mark them as you remove them, or run the archive script in `scripts/` to move them into `archived/docs/`.

- [ ] context7-mcp-integration.md
- [ ] docker-integration.md
- [ ] quickstart.md

To archive all at once (PowerShell):

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\move_docs_to_archive.ps1
```
