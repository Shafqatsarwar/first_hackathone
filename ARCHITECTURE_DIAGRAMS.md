# Visual Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACES                          │
├──────────────────────┬──────────────────────┬────────────────────┤
│   Standalone HTML    │   React Component    │  Custom Frontend   │
│  chatbot_widget.     │  ChatbotWidget.jsx   │   (Your App)       │
│         html         │                      │                    │
└──────────┬───────────┴──────────┬───────────┴────────┬───────────┘
           │                      │                     │
           └──────────────────────┼─────────────────────┘
                                  │
                        ┌─────────▼─────────┐
                        │   FastAPI (8000)  │
                        │   api/main.py     │
                        └─────────┬─────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │  api/routers/chat.py      │
                    │  - /query (RAG fallback)  │
                    │  - /agent-query (NEW)     │
                    │  - /modules               │
                    │  - /health                │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │  chatbot/agent.py         │
                    │  Agent Workflow Engine    │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────┴────────┬──────────────┐
                    │                      │              │
            ┌───────▼──────┐      ┌────────▼────┐  ┌──────▼────────┐
            │  OpenAI API  │      │ File Search │  │  Web Search   │
            │              │      │   (Book)    │  │   (Internet)  │
            │ - gpt-4      │      │             │  │               │
            │ - gpt-3.5    │      │ Vector DB   │  │ Google Search │
            │ - Reasoning  │      │ Upload      │  │ API           │
            └──────────────┘      └─────────────┘  └───────────────┘
```

---

## Request/Response Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    USER ASKS A QUESTION                     │
│                  "What is physical AI?"                     │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  Browser/Frontend (Widget)       │
        │  - Validates input               │
        │  - Sends via POST /agent-query   │
        │  - Shows loading indicator       │
        └──────────┬───────────────────────┘
                   │
                   ▼ HTTP POST
        ┌──────────────────────────────────┐
        │  FastAPI Router (chat.py)        │
        │  - Receives request              │
        │  - Validates ChatRequest model   │
        │  - Creates WorkflowInput         │
        └──────────┬───────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────┐
        │  Agent Initialization (agent.py) │
        │  - Load tools                    │
        │  - Configure model               │
        │  - Set instructions              │
        └──────────┬───────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────┐
        │  Agent Reasoning Phase           │
        │  - Analyze question              │
        │  - Choose tools:                 │
        │    A) File Search first?         │
        │    B) Need Web Search?           │
        │    C) Use both?                  │
        └──────────┬───────────────────────┘
                   │
        ┌──────────┴──────────────┐
        │                         │
        ▼                         ▼
    ┌──────────┐          ┌───────────────┐
    │ File Srch│          │  Web Search   │
    │ (Book)   │          │ (Internet)    │
    │          │          │               │
    │ Search   │          │ Google API    │
    │ vector DB│          │               │
    │ Returns  │          │ Returns       │
    │ related  │          │ web results   │
    │ sections │          │               │
    └──────────┘          └───────────────┘
        │                         │
        └──────────┬──────────────┘
                   │
                   ▼
        ┌──────────────────────────────────┐
        │  OpenAI Model Processing         │
        │  - Combine sources               │
        │  - Generate response             │
        │  - Add reasoning                 │
        │  - Format sources                │
        └──────────┬───────────────────────┘
                   │
                   ▼ Response Object
        ┌──────────────────────────────────┐
        │  {                               │
        │    "output_text": "...",         │
        │    "reasoning": "...",           │
        │    "sources": [...]              │
        │  }                               │
        └──────────┬───────────────────────┘
                   │
                   ▼ JSON
        ┌──────────────────────────────────┐
        │  Router Returns Response         │
        │  - Status 200                    │
        │  - JSON body                     │
        └──────────┬───────────────────────┘
                   │
                   ▼ HTTP Response
        ┌──────────────────────────────────┐
        │  Widget Displays Response        │
        │  - Hide loading indicator        │
        │  - Show bot message              │
        │  - Display sources               │
        │  - Auto-scroll to message        │
        └──────────────────────────────────┘

        ┌──────────────────────────────────┐
        │      USER SEES ANSWER            │
        │   WITH SOURCES & REASONING       │
        └──────────────────────────────────┘
```

---

## Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                         │
├─────────────────────┬──────────────────┬──────────────────────┤
│ chatbot_widget.html │ ChatbotWidget.   │ Custom Integration   │
│                     │ jsx (React)      │ (Your code)          │
└─────────────────────┴──────────────────┴──────────────────────┘
                              ▲
                              │ HTTP
                              │ REST
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                      API LAYER (FastAPI)                       │
├─────────────────────────────────────────────────────────────────┤
│  main.py                                                        │
│  ├── CORS Middleware                                           │
│  ├── Error Handling                                            │
│  └── Route Registration                                        │
│                                                                 │
│  routers/chat.py                                               │
│  ├── POST /query           → RAG fallback                      │
│  ├── POST /agent-query     → Agent workflow (NEW)              │
│  ├── GET /modules          → Module metadata                   │
│  └── GET /health           → Health check                      │
└────────────────────┬───────────────────────────────────────────┘
                     │ Import & Async Call
                     ▼
┌────────────────────────────────────────────────────────────────┐
│                  BUSINESS LOGIC LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│  chatbot/agent.py                                               │
│  ├── WorkflowInput       (Pydantic model)                      │
│  ├── initialize_agent()  (Agent config)                        │
│  │   ├── FileSearchTool  (Vector store)                        │
│  │   └── WebSearchTool   (Search API)                          │
│  ├── run_workflow()      (Async execution)                     │
│  └── Returns: output_text, reasoning, sources                  │
└────────────────────┬───────────────────────────────────────────┘
                     │ API Calls
                     ▼
┌────────────────────────────────────────────────────────────────┐
│                  EXTERNAL SERVICES                              │
├─────────────────────┬──────────────────┬──────────────────────┤
│ OpenAI (gpt-4,      │ Vector Store     │ Web Search API       │
│ reasoning, etc)     │ (Your Book)      │ (Google/Bing)        │
└─────────────────────┴──────────────────┴──────────────────────┘
```

---

## Data Models (Pydantic)

```
┌─────────────────────────────────────────────────────┐
│        FROM FRONTEND (ChatRequest)                  │
├─────────────────────────────────────────────────────┤
│ {                                                   │
│   "message": str,           # Required              │
│   "user_id": str | None     # Optional              │
│ }                                                   │
└──────────────────────┬────────────────────────────┘
                       │ Converted to
                       ▼
┌─────────────────────────────────────────────────────┐
│       AGENT WORKFLOW (WorkflowInput)                │
├─────────────────────────────────────────────────────┤
│ {                                                   │
│   "input_as_text": str      # User query            │
│ }                                                   │
└──────────────────────┬────────────────────────────┘
                       │ Processed by Agent
                       │ Returns
                       ▼
┌─────────────────────────────────────────────────────┐
│        TO FRONTEND (Chat Response)                  │
├─────────────────────────────────────────────────────┤
│ {                                                   │
│   "response": str,          # Answer               │
│   "sources": list,          # References           │
│   "reasoning": str|None,    # How it decided      │
│   "user_id": str|None       # Echo back            │
│ }                                                   │
└─────────────────────────────────────────────────────┘
```

---

## File Organization

```
project-root/
│
├── 📁 chatbot/                      ← Core chatbot module
│   ├── __init__.py
│   ├── agent.py                     ← ⭐ NEW - Agent configuration
│   ├── chatbot_widget.html          ← ⭐ NEW - HTML widget
│   ├── ChatbotWidget.jsx            ← ⭐ NEW - React component
│   ├── rag.py                       ← RAG system (existing)
│   ├── env_utils.py                 ← Environment helpers
│   └── overview.md
│
├── 📁 api/                          ← API layer
│   ├── main.py                      ← FastAPI app
│   ├── overview.md
│   ├── 📁 models/
│   ├── 📁 routers/
│   │   ├── chat.py                  ← ✏️ MODIFIED - Added /agent-query
│   │   ├── auth.py
│   │   ├── personalization.py
│   │   └── i18n.py
│   └── 📁 db/
│
├── 📁 docs/                         ← Documentation
├── 📁 modules/                      ← Book content
├── 📁 scripts/                      ← Utilities
│
├── 📄 .env.example                  ← ⭐ NEW - Environment template
├── 📄 requirements.txt               ← ✏️ MODIFIED - Added dependencies
├── 📄 CHATBOT_INTEGRATION_GUIDE.md   ← ⭐ NEW - Full documentation
├── 📄 QUICKSTART_CHATBOT.md          ← ⭐ NEW - Quick start
├── 📄 CHATBOT_SUMMARY.md             ← ⭐ NEW - Summary overview
├── 📄 CHATBOT_SETUP_COMPLETE.md      ← ⭐ NEW - Setup completion guide
├── 📄 DEPLOYMENT_GUIDE.md            ← ⭐ NEW - Deployment instructions
├── 📄 test_agent_chatbot.py          ← ⭐ NEW - Test suite
│
└── 📄 README.md                     ← Existing project docs

⭐ = New files
✏️ = Modified files
```

---

## Widget User Interaction Flow

```
┌─────────────────────────────────────────────────┐
│         PAGE LOADS                              │
│  Widget invisible, button visible in corner     │
└─────────────┬───────────────────────────────────┘
              │
              ▼ User clicks button
┌─────────────────────────────────────────────────┐
│  WIDGET APPEARS                                 │
│  ├── Slides up animation                        │
│  ├── Shows welcome message                      │
│  └── Input field focused                        │
└─────────────┬───────────────────────────────────┘
              │
              ▼ User types message
┌─────────────────────────────────────────────────┐
│  INPUT ACTIVE                                   │
│  ├── Textarea grows/shrinks                     │
│  ├── Send button enabled                        │
│  └── Hint text visible                          │
└─────────────┬───────────────────────────────────┘
              │
              ▼ User presses Send or clicks button
┌─────────────────────────────────────────────────┐
│  MESSAGE SENT                                   │
│  ├── Message appears in chat (user side)        │
│  ├── Input cleared                              │
│  ├── Loading indicator shows                    │
│  └── Send button disabled                       │
└─────────────┬───────────────────────────────────┘
              │
              ▼ Agent processing (async)
┌─────────────────────────────────────────────────┐
│  AGENT THINKING                                 │
│  ├── File search runs                           │
│  ├── Web search runs (if needed)                │
│  ├── Model processes                            │
│  └── Reasoning generated                        │
└─────────────┬───────────────────────────────────┘
              │
              ▼ Response received
┌─────────────────────────────────────────────────┐
│  RESPONSE DISPLAYED                             │
│  ├── Loading indicator removed                  │
│  ├── Bot message appears (bot side)             │
│  ├── Auto-scrolls to message                    │
│  ├── Sources shown if available                 │
│  └── Input re-enabled                           │
└─────────────┬───────────────────────────────────┘
              │
              ▼ User continues or closes
┌─────────────────────────────────────────────────┐
│  READY FOR NEXT MESSAGE                         │
│  or                                             │
│  User clicks button to close widget             │
│  - Widget slides down (hides)                   │
│  - Button remains visible                       │
│  - Chat history preserved                       │
└─────────────────────────────────────────────────┘
```

---

## State Management (React Component)

```
ChatbotWidget
├── State:
│   ├── isOpen: boolean           # Widget visibility
│   ├── messages: Message[]       # Chat history
│   ├── input: string             # Current input text
│   └── isLoading: boolean        # API request in progress
│
├── Refs:
│   └── messagesEndRef            # Auto-scroll reference
│
├── Effects:
│   └── useEffect()
│       └── Auto-scroll to latest message
│
├── Handlers:
│   ├── handleSendMessage()       # Send logic
│   ├── handleKeyDown()           # Enter key handling
│   └── Internal API fetching
│
└── Render:
    ├── Floating button
    ├── Chat container (conditional)
    ├── Header
    ├── Messages list
    ├── Loading indicator (conditional)
    └── Input area
```

---

## Error Handling Flow

```
┌──────────────────────────┐
│   User sends query       │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐     No
│ Agents package           ├──────────→ Return error
│ installed?               │           "Install agents"
└────────┬─────────────────┘
         │ Yes
         ▼
┌──────────────────────────┐     No
│ API key                  ├──────────→ Return error
│ configured?              │           "Missing OPENAI_API_KEY"
└────────┬─────────────────┘
         │ Yes
         ▼
┌──────────────────────────┐     No
│ Vector store ID          ├──────────→ Warning (fallback to web)
│ configured?              │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Run agent workflow       │
└────────┬─────────────────┘
         │
    ┌────┴────┐
    │          │
    ▼          ▼
Success     Error
    │          │
    │          ▼
    │      ┌──────────────────┐
    │      │ Log error        │
    │      │ Return user-     │
    │      │ friendly message │
    │      └──────────────────┘
    │          │
    └──────┬───┘
           │
           ▼
    ┌──────────────────┐
    │ Return response  │
    │ to frontend      │
    └──────────────────┘
```

---

## Deployment Options

```
┌─────────────────────────────────────────────────────┐
│           YOUR CHATBOT APPLICATION                 │
└────────┬────────────────────────────────────────────┘
         │
    ┌────┴─────────────────────────────────┐
    │                                       │
    ▼                                       ▼
┌──────────────────┐              ┌────────────────────┐
│    DOCKER        │              │  CLOUD NATIVE      │
├──────────────────┤              ├────────────────────┤
│                  │              │                    │
│ ├─ Local Dev     │              │ ├─ Heroku          │
│ ├─ Docker Compose│              │ ├─ AWS EB          │
│ ├─ Registry Push │              │ ├─ Google Cloud Run│
│ └─ Production    │              │ └─ Azure App Srvc  │
└──────────────────┘              └────────────────────┘
    │                                   │
    ▼                                   ▼
┌──────────────────┐              ┌────────────────────┐
│    VPS           │              │  SERVERLESS        │
├──────────────────┤              ├────────────────────┤
│                  │              │                    │
│ ├─ AWS EC2       │              │ ├─ AWS Lambda      │
│ ├─ DigitalOcean  │              │ ├─ Google Functions│
│ ├─ Linode        │              │ └─ Azure Functions │
│ └─ Custom Server │              │                    │
└──────────────────┘              └────────────────────┘

All with:
├─ Environment variables
├─ SSL/TLS certificates
├─ Monitoring & logging
├─ Rate limiting
└─ CORS configuration
```

---

## Summary

This comprehensive integration adds an intelligent chatbot system to your project with:

1. **Smart Agent** - Reasons about which tools to use
2. **Multiple Interfaces** - HTML, React, or API
3. **Professional UI** - Sticky button with modern chat
4. **Production Ready** - Security, monitoring, deployment
5. **Well Documented** - Guides, examples, tests

All seamlessly integrated with your existing FastAPI backend!

---

*Diagrams created December 2024*
